if (x > 0) entao {
    integer y = 15;
    string mensagem = "X é positivo!";
} elseif(x < 0) {
    string mensagem = "X não é positivo.";
}else{
    string mensagem = "X é igual a zero"
}

// Comentário: Isso é um comentário.